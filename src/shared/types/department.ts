export interface Department {
    department_name: string;
    department_mnemonic: string;
    bridge_url: string;
    spar_url: string;
    pbms_url: string;
    registry_url: string;
    superset_url: string;
}
